**Feature Lifecycle**: 
* Before touching files, list every file you plan to modify. If the list is >5, stop and propose splitting the task.
* For complex refactors, create an **ExecPlan** in `.agents/PLANS.md` before implementation. Stop and ask immediately once this file is created before continuing to execute any of the remaining plan or steps.